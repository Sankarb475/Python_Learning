create KEYSPACE Employee
with replication={'class':'SimpleStrategy','replication_factor':1};

use Employee;

create table data(
id int PRIMARY KEY,
name text,
monthly_salary bigint, 
annual_salary_rupee bigint, 
Currency text
);
